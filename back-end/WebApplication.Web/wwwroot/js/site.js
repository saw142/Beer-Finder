// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.
//document.addEventListener('DOMContentLoaded', () => {
//    document.querySelector("#SearchText").addEventListener("keyup", event => {
//        checkNewButtonDisable(event.currentTarget, document.querySelector("#SearchButton"));
//    });

//});

    /**
 * Check to see if the text box has a value in it and toggle the enable
 * status on the corresponding button because of it.
 * 
 * @param {Event} event the event object
 */
//function checkNewButtonDisable(inputBox, button) {
    //let button = inputBox.nextElementSibling;

    //if (inputBox.value !== '') {
    //    button.removeAttribute('disabled');
    //} else {
    //    button.setAttribute('disabled', true);
    //}
//}